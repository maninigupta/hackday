<h1>All users</h1>

<ul class="users">
	<% @users.each do |user| %>
	  <li>
			<ziggeo ziggeo-video="<%= user.videotoken %>" 
        ziggeo-width=320
        ziggeo-height=240>
			</ziggeo>
	    <%= user.name %>
	  </li>
	<% end %>
</ul>